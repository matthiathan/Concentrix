# Retro UI Preview

The retro redesign lives on `retro-ui-superdesign`.

Rollback branches:
- `main`
- `checkpoint-pre-retro-ui`

The redesign is presentation-only and does not change Supabase logic, RLS, imports, authentication, reports, machine scanning, or operational workflows.